CREATE TABLE PRODUCT_CATEGORY
(
  PRODUCT_CATEGORY_NAME VARCHAR(255) NOT NULL PRIMARY KEY,
  DESCRIPTION           VARCHAR(255),
  CREATED_DATE          TIMESTAMP
);

CREATE TABLE PRODUCT
(
  PRODUCT_ID            BIGINT           NOT NULL PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  SKU_NUMBER            VARCHAR(25),
  PRODUCT_NAME          VARCHAR(255),
  DESCRIPTION           VARCHAR(255),
  INVENTORY_COUNT       INTEGER,
  PRICE                 DOUBLE PRECISION,
  CREATED_DATE          TIMESTAMP,

  PRODUCT_CATEGORY_NAME VARCHAR(255)
    CONSTRAINT FK_PRODUCT_PRODUCT_CATEGORY
    REFERENCES PRODUCT_CATEGORY
);

CREATE TABLE CART
(
  CART_ID      BIGINT       NOT NULL PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  CART_NAME    VARCHAR(255),
  DESCRIPTION  VARCHAR(255),
  CREATED_DATE TIMESTAMP
);

CREATE TABLE ITEM
(
  ITEM_ID      BIGINT  NOT NULL PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  QUANTITY     INTEGER,
  CREATED_DATE TIMESTAMP,

  PRODUCT_ID   BIGINT
    CONSTRAINT FK_ITEM_PRODUCT
    REFERENCES PRODUCT,

  CART_ID      BIGINT
    CONSTRAINT FK_ITEM_CART
    REFERENCES CART
);